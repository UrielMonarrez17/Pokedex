<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokedex</title>
    <link rel="stylesheet" href="/style.css">
    
</head>
<body>
    <!-- Barra estática alrededor de la página -->
    <div class="outer-bar">
        <div class="container">
            <!-- Zona de visualización del Pokémon seleccionado -->
            <div class="pokemon-display">
                <div class="pokemon-name"><%= pokemons[0].name %></div>
                <div class="image-container">
                    <div class="red-background"></div>
                    <img src="<%= pokemons[0].image %>" alt="<%= pokemons[0].name %>">
                </div>
                <div class="base-stats">
                    <h3>Base Stats</h3>
                    <div class="stat">
                        <span>HP</span>
                        <span>150/300</span>
                    </div>
                    <div class="stat">
                        <span>ATK</span>
                        <span>150/300</span>
                    </div>
                    <div class="stat">
                        <span>DEF</span>
                        <span>150/300</span>
                    </div>
                    <div class="stat">
                        <span>SPD</span>
                        <span>150/300</span>
                    </div>
                </div>
                <p>Type: <%= pokemons[0].type %></p>
            </div>

            <!-- Barra lateral con los nombres de los Pokémon -->
            <div class="sidebar">
                <ul id="pokemonList">
                    <% pokemons.forEach(pokemon => { %>
                        <li data-id="<%= pokemon.id %>">
                            <%= pokemon.id %>. <%= pokemon.name %>
                        </li>
                    <% }) %>
                </ul>
            </div>
        </div>
    </div>
</body>
</html>

